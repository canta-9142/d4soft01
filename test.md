# 手動テスト項目

## 1. 目的と判定方針

`spec.md` に定義された Canvas 型 ToDo アプリの仕様どおりに動作することを、ブラウザ上の手動操作で確認する。

- テスト対象仕様: `spec.md`（特に 3 章～5 章）
- 補助確認: `req.md` の非機能要求
- 実施方法: 画面操作を基本とし、保存内容、ID、日時、座標などはブラウザの開発者ツールを併用する
- 優先度:
  - A: 基本機能、データ消失、仕様の主要機能に関わるため必須
  - B: 操作性、例外処理、機能間の組み合わせに関わるため実施推奨
  - C: 非機能、互換性、上限などの補足確認
- 合格条件: 各項目の期待結果をすべて満たし、操作中に未処理例外、画面停止、意図しないデータ変更が発生しないこと

## 2. テスト環境と記録方法

### 2.1 推奨環境

- 最新版の Chrome または Edge を主環境とする
- 互換性確認では Firefox も使用する
- 開発者ツールの Console、Application（Local Storage）、Elements を利用できる状態にする
- OS、ブラウザ名・バージョン、画面サイズ、実施日、実施者を記録する

### 2.2 結果の記録

各テスト項目について、結果を `OK / NG / 未実施 / 対象外` で記録する。NG の場合は、実際の結果、再現手順、スクリーンショット、Console のエラー、使用した保存データを残す。

### 2.3 初期化方法

完全な初期状態から開始する項目では、開発者ツールで Local Storage の `d4soft01.todoCanvas.state` を削除してから再読み込みする。別項目の未保存データや取り消し履歴を持ち越さないようにする。

## 3. 共通テストデータ

### 3.1 キャンバス

- C1: `レポート作成`
- C2: `個人開発`
- C3: `同名キャンバス`
- C4: `同名キャンバス`（C3 と同名）

### 3.2 タスク

| 記号 | タイトル | 説明文 | ステータス |
|---|---|---|---|
| A | 要件確認 | Reportの要件を読む | 未着手 |
| B | 資料収集 | 参考文献を集める | 進行中 |
| C | 本文作成 | report BODYを書く | 完了済み |
| D | 校正 | 誤字を確認する | 未着手 |
| E | 提出 | LMSから提出 | 完了済み |
| F | 孤立タスク | 接続なし | 未着手 |
| G | 要件確認 | 重複タイトル確認用 | 未着手 |

### 3.3 接続グラフ

深さの確認では、C1 に次の接続を作る。

```text
A → B → C → D
↑       │   │
└───────┘   → E

F（接続なし）
```

作成する接続は `A→B`、`B→C`、`C→A`、`C→D`、`D→E`。これにより循環、向きを無視した深さ、最短経路、到達不能をまとめて確認できる。必要な項目では `A→C` を追加する。

### 3.4 入力値

- 空文字: ``
- 空白のみ: 半角空白3文字、全角空白3文字、タブのみ
- HTML風文字列: `<img src=x onerror=alert(1)>`
- スクリプト風文字列: `<script>alert('x')</script>`
- 検索用: `report`、` REPORT `、`本文`、`れぽーと`
- 深さ: `0`、`1`、`2`、空欄、`-1`、`1.5`、`abc`

## 4. 初期状態・画面表示

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| INIT-01 | 2.4, 5.9, 5.10 | A | 保存データを削除して再読み込みする | キャンバス0個、現在キャンバス未設定、通常モードで起動し、検索・ステータス・深さフィルターは無効である |
| INIT-02 | 5.10 | A | キャンバス0個の状態で、タスク追加、検索、接続、編集、削除、キャンバス移動、切り替えを試す | 禁止対象の操作は実行されず、例外も発生しない。新規キャンバス作成の入口だけは操作できる |
| UI-01 | 3.1～3.3 | A | C1 を作成して画面全体を確認する | 上部にハンバーガーメニュー、現在の状態、タスク件数、保存状態、保存操作があり、主領域は格子状のキャンバスである |
| UI-02 | 3.3 | A | A～E を登録し、各ステータスを設定する | 表示対象について合計、未着手、進行中、完了済みの件数が正しい |
| UI-03 | 3.3, 5.9 | A | タスクを変更し、その後保存する | 変更後は「未保存」、保存成功後は保存完了または変更なしと表示される |
| UI-04 | 3.4 | B | 未選択カード、選択カード、編集カードを順に確認する | 未選択はタイトルとステータスだけ、選択時は説明文を含めて拡大・前面表示、編集時は3属性を編集できる |
| UI-05 | 3.5 | A | A→B を接続して線を確認し、線を選択する | カード中央同士が線で結ばれ、中央の矢印が A から B を向く。選択線は色または線幅で区別できる |
| UI-06 | 3.6 | B | タスクとキャンバスをそれぞれ右クリックし、メニュー外をクリックする | タスクメニューに編集・コピー・削除、キャンバスメニューに新規タスク・新規キャンバス・タイトル変更・削除・復元があり、外側クリックで閉じる |
| UI-07 | 3.6, 5.6, 5.10 | A | タスク削除、キャンバス削除を開始する | 確認表示に削除対象のタイトルが含まれ、対象を取り違えない |

## 5. キャンバス

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| CAN-01 | 2.4, 5.10 | A | 初期状態から新規キャンバスを作成する | タイトルが `新規キャンバス`、表示位置が (0, 0) の空キャンバスが作られ、現在キャンバスになる |
| CAN-02 | 2.4, 5.10 | B | C3、C4 のように同じタイトルのキャンバスを作成する | 同名キャンバスを複数作成でき、それぞれ別のキャンバスとして扱われる |
| CAN-03 | 5.10 | A | タイトルを `レポート作成` に変更する | 表示名が変わり、未保存状態になる。開発者ツール確認では作成日時は不変、更新日時だけが新しくなる |
| CAN-04 | 5.10 | A | タイトルを空文字、半角空白のみ、全角空白のみへ変更する | 変更は拒否され、直前の有効なタイトルと他の状態を保持し、エラーを表示する |
| CAN-05 | 3.2, 5.10 | A | 空白部分をドラッグしてキャンバスをパンする | 表示全体が移動するが、タスクのキャンバス座標は変化しない。キャンバス表示位置と更新日時が更新される |
| CAN-06 | 4.3, 5.1 | B | タスク、接続線、メニュー、入力欄からドラッグを開始する | 空白以外からの操作でキャンバスが意図せず移動しない |
| CAN-07 | 4.1, 5.9, 5.10 | A | C1 と C2 を作り、C1 にタスクを登録後、メニューで C2、C1 の順に切り替える | 切り替え先だけが表示され、タスクと接続はキャンバスごとに分離される。切り替え前保存が成功した場合だけ切り替わる |
| CAN-08 | 4.2 | B | 3キャンバスで Shift+↓、Shift+↑ を繰り返す | 一覧順に次／前のキャンバスへ切り替わる。選択、メニュー、モードの状態が矛盾しない |
| CAN-09 | 5.10 | A | C1、C2、C3 の順で並べ、C2 を削除して確定する | C2 と配下の全タスク・接続が削除され、次の C3 が現在キャンバスになる |
| CAN-10 | 5.10 | A | 一覧末尾の C3 を削除して確定する | 次がないため、削除前に1つ前だったキャンバスが現在キャンバスになる |
| CAN-11 | 5.10 | A | 最後の1キャンバスを削除して確定する | キャンバス0個、現在キャンバス未設定となり、検索・深さフィルターが解除され、選択、メニュー、検索ウィンドウも閉じる |
| CAN-12 | 5.10 | A | タスクと接続を持つキャンバスの削除確認でキャンセルする | キャンバス、タスク、接続、表示設定、選択状態のデータは変更されない |
| CAN-13 | 5.10, 5.11 | A | キャンバス削除後に Ctrl+Z、その後 Ctrl+Y を行う | Undo で元の一覧位置、配下データ、現在キャンバス、表示設定、選択状態まで戻り、Redo で再度仕様どおり削除される |

## 6. タスク登録・選択・表示

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| TASK-01 | 5.2 | A | ボタンまたはキャンバスメニューから、タイトルのみ入力して登録する | 説明文なしで登録でき、初期ステータスは未着手、位置は表示中央付近または操作位置になる |
| TASK-02 | 4.2, 5.2 | A | Ctrl+A で登録画面を開き、有効な内容を登録する | 現在表示中のキャンバス中央付近にタスクが作られる |
| TASK-03 | 4.3, 5.2 | A | 空白位置をダブルクリックして登録する | ダブルクリックしたキャンバス座標を左上位置としてタスクが作られる |
| TASK-04 | 5.2 | A | タイトルを空欄、半角空白のみ、全角空白のみとして登録を試す | 登録されずエラーが表示される。既存タスク、接続、表示設定は変化しない |
| TASK-05 | 2.1, 5.2 | B | A と G のように同じタイトルのタスクを登録する | 重複タイトルでも両方登録できる |
| TASK-06 | 2.1, 2.4, 5.2 | B | 登録直後に Local Storage またはアプリ状態を確認する | 一意なタスクIDが発行され、作成日時と更新日時は登録時点のISO 8601文字列である |
| TASK-07 | 5.2 | A | 検索、ステータス絞り込み、深さフィルターをすべて有効にした状態で新規タスクを登録する | 全フィルターが解除され、検索文字列は空、ステータスは絞り込みなし、深さ基準と最大深さも未設定になり、新規タスクが見える |
| TASK-08 | 5.3 | A | A をクリックし、次に B をクリックし、最後に空白をクリックする | 選択は常に最大1件。B 選択時に A は解除され、空白クリックで B も解除される |
| TASK-09 | 5.3 | B | タスクを選択後に接続線を選択し、その後別タスクを選択する | 接続線選択時はタスク選択が解除され、タスク選択時は接続線選択が解除される |
| TASK-10 | 4.3 | B | 2枚のカードを重ね、重なった部分をクリックする | 最前面のカードだけが操作対象になる |
| TASK-11 | 4.3 | C | カードにマウスを乗せ、外す | ホバー中であることが視覚的に分かり、選択や位置が勝手に変化しない |

## 7. タスク編集・移動・削除

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| EDIT-01 | 4.2～4.3, 5.4 | A | タスクをダブルクリック、または選択後 Ctrl+E／編集メニューを使う | 編集モードになり、既存のタイトル、説明文、ステータスが入力欄へ表示される |
| EDIT-02 | 5.4 | A | 3属性を変更して確定ボタンまたはタイトル欄の Enter で確定する | 変更内容が表示へ反映され、通常モードに戻り、更新日時が新しくなる |
| EDIT-03 | 4.2, 5.4 | A | 説明文入力欄で Enter を押す | 確定せず説明文に改行が入力される |
| EDIT-04 | 5.4 | A | タイトルを空欄または空白のみにして確定する | 確定できずエラーが表示され、元のタスク内容を保持する |
| EDIT-05 | 4.2, 5.4 | A | 編集内容を変更後、キャンセルボタンと Esc をそれぞれ試す | 変更前の内容と更新日時を保持して通常モードに戻る |
| EDIT-06 | 5.4, 5.8 | B | `report` 検索中に一致しているタスクを編集し、一致しないタイトル・説明へ変える | 編集自体は確定し、その直後に対象タスクは非表示、選択も解除される |
| MOVE-01 | 5.5 | A | 通常モードでタスクをドラッグする | ドロップ位置へ移動し、位置と更新日時が更新され、未保存になる |
| MOVE-02 | 5.5 | A | 接続済みタスクをドラッグする | 移動中も接続線がカード中心へ追従し、移動後も向きが正しい |
| MOVE-03 | 5.5 | B | タスクを別タスクの上へ移動する | 重なりが許可され、位置が勝手に補正されない |
| DEL-01 | 4.2, 5.6 | A | タスクを選択して Delete、Ctrl+D、タスクメニューの削除をそれぞれ試し、確認する | 指定したタスクが削除され、選択解除される |
| DEL-02 | 5.6 | A | A→B、B→C の状態で B を削除する | B と A→B、B→C が同時に削除され、A と C は残る |
| DEL-03 | 5.6 | A | 削除確認でキャンセルする | タスク、関連接続、選択、表示設定は変化しない |
| DEL-04 | 5.6, 5.8 | A | 深さフィルターの基準タスクを削除する | 深さフィルターも解除される |
| DEL-05 | 5.6, 5.11 | A | 接続を持つタスクを削除後、Undo、Redo する | Undo で同じタスク・関連接続・選択・深さ設定が復元され、Redo で再削除される |

## 8. モードと接続

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| MODE-01 | 5.1 | A | 起動、編集開始／終了、接続開始／終了を行う | メニューバーのモード表示が常に実際の操作状態と一致する |
| MODE-02 | 5.1 | A | 接続モード中にタスク移動・編集・削除・キャンバス移動を試す | すべて実行されない |
| MODE-03 | 5.1 | A | 編集モード中にタスク移動・接続・削除・キャンバス移動を試す | すべて実行されない |
| MODE-04 | 4.2, 5.1 | A | 接続モード中と編集中に Esc を押す | 操作をキャンセルして通常モードへ戻る。編集中の変更は保存されない |
| CON-01 | 5.1, 5.7 | A | 接続モードで A、B の順に選択する | A を接続元として強調後、A→B が作成される。作成後も接続モードは継続するが接続元選択は解除される |
| CON-02 | 5.1, 5.7 | A | 接続元 A を選び、もう一度 A を選ぶ | 自己接続は作られず、エラーを表示し、A を接続元として保持する |
| CON-03 | 5.7 | A | 既存 A→B と同じ順で再接続する | 重複接続は作られず、重複であることが表示される |
| CON-04 | 5.7 | A | A→B がある状態で B、A の順に選ぶ | 逆向き B→A は別接続として作成できる |
| CON-05 | 5.7 | A | A→B、B→C、C→A を作る | 循環接続を作成でき、画面停止や例外がない |
| CON-06 | 5.7, 5.10 | A | C1 のタスクを接続元に選び、C2 へ切り替えてタスクを選ぶ | キャンバスを越えた接続は作成されず、接続元選択も持ち越されない |
| CON-07 | 5.7 | A | 接続線をクリックし Delete を押して確認する | 接続だけが削除され、両端タスクは残る |
| CON-08 | 5.7 | A | 接続削除の確認をキャンセルする | 接続とタスクは変更されない |
| CON-09 | 5.7, 5.11 | A | 接続作成、Undo、Redo、接続削除、Undo、Redo を順に行う | 各操作単位で接続だけが正しく追加・削除される |

## 9. 検索・ステータス・深さフィルター

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| FIL-01 | 5.8 | A | Ctrl+F とメニューバー操作で検索ウィンドウを開く | 検索文字列、ステータス、基準タスク、基準に設定、最大深さ、有効／無効、クリア操作が表示される |
| FIL-02 | 5.8 | A | `report`、` REPORT ` を順に入力する | 前後空白と大文字小文字を無視し、タイトルまたは説明文に部分一致する A、C だけが即時表示される |
| FIL-03 | 5.8 | B | `本文`、空欄、空白のみ、複数語、正規表現風文字列を入力する | 日本語も部分一致する。空欄・空白だけなら検索なし。複数語や正規表現は特別解釈せず1文字列として扱う |
| FIL-04 | 5.8 | B | 全角半角違い、ひらがな／カタカナ違いの文字列を検索する | 仕様どおり同一視せず、実際に部分一致するものだけ表示する |
| FIL-05 | 5.8 | A | 検索で片端を非表示にする | 非表示タスクを選択・移動できず、そのタスクにつながる線も非表示になる |
| FIL-06 | 5.8 | A | 検索条件クリアまたは Ctrl+Shift+F を実行する | 検索欄が空になり、検索だけが解除される |
| FIL-07 | 5.8 | A | ステータスを絞り込みなし、未着手、進行中、完了済みに順次変更する | 変更が即時反映され、指定ステータスだけが表示される。絞り込みなしでは全件表示される |
| FIL-08 | 5.8 | A | タスク未選択で検索ウィンドウを開く | 「基準に設定」が無効である |
| FIL-09 | 5.8 | A | A を選択して検索ウィンドウを開き、基準に設定する | 基準タスクとして A のタイトルまたは識別可能な表示が出る |
| FIL-10 | 5.8 | A | 基準未設定で深さフィルターを有効にする | 適用されず入力エラーが表示され、直前の表示対象を保持する |
| FIL-11 | 5.8 | A | A を基準として最大深さ `0`、`1`、`2` を順に適用する | 0 は A のみ、1 は A・B・C、2 は A・B・C・Dを表示する。F は常に非表示である |
| FIL-12 | 5.8 | A | 最大深さを空欄、`-1`、`1.5`、`abc` にして有効化する | いずれも適用されず入力エラーとなり、不正値で表示状態を壊さない |
| FIL-13 | 2.1, 5.8 | A | C→A がある状態で A 基準・深さ1を適用する | 接続方向を無視するため C も深さ1として表示される |
| FIL-14 | 5.8 | A | A→B→C と A→C の両方を作り、A 基準・深さ1を適用する | C は最短経路の深さ1として表示される |
| FIL-15 | 5.7, 5.8 | A | 循環グラフで最大深さを大きな値にする | 同じタスクを無限探索せず短時間で結果が表示される |
| FIL-16 | 5.8 | B | 有効な基準と最大深さを設定し、深さフィルターを無効→再有効にする | 無効中は深さで絞り込まず、基準IDと最大深さの入力値は保持され、再有効化で再利用される |
| FIL-17 | 5.8 | A | 深さ適用中に検索で基準 A を非表示にする | 深さフィルター自体は有効のままで、検索条件と深さ条件の両方を満たすものだけ表示される |
| FIL-18 | 5.8 | A | 検索 `report`、ステータス `完了済み`、A基準・深さ2を併用する | 3条件をすべて満たすタスクだけが表示される。両端表示の線だけが表示され、件数も表示対象に一致する |
| FIL-19 | 5.3, 5.8 | B | 選択中タスクが非表示になる検索またはフィルターを設定する | そのタスクの選択状態が解除される |
| FIL-20 | 5.8 | B | 検索ウィンドウ外クリックと Esc で閉じる | ウィンドウだけが閉じ、検索、ステータス、深さの設定は保持される |

## 10. コピー・貼り付け・Undo・Redo

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| HIST-01 | 4.2, 5.11 | A | A を選択して Ctrl+C、Ctrl+V を押す | 同じタイトル・説明・ステータス、新しいID・日時のタスクが、A から x・y 各24pxずれた位置に作られる |
| HIST-02 | 5.11 | A | A を C1 でコピーし、C2 に切り替えて貼り付ける | C2 の表示中央付近に新規タスクとして貼り付けられ、C1 の接続やIDはコピーされない |
| HIST-03 | 5.11 | B | コピー元タスクまたはコピー元キャンバスを削除してから別キャンバスへ貼り付ける | コピー時の3属性で貼り付けでき、位置は現在表示の中央付近になる |
| HIST-04 | 5.11 | A | アプリ起動後に何もコピーせず Ctrl+V を押す | タスクは作られず、貼り付けできない旨が表示される |
| HIST-05 | 4.2, 5.11 | A | 編集中の入力欄で文字列を選択し Ctrl+C／Ctrl+V を使う | ブラウザ標準の文字列コピー・貼り付けとなり、タスク複製は発生しない |
| HIST-06 | 5.11 | A | 登録、編集、移動、削除、接続作成、接続削除、貼り付け、キャンバス削除の各直後に Ctrl+Z／Ctrl+Y を試す | 8種類すべてが操作単位で取り消し／やり直しできる |
| HIST-07 | 5.11 | A | 保存、検索変更、ステータス変更、深さ変更、キャンバス切り替え後に Ctrl+Z する | これら対象外操作自体は取り消されず、直前の履歴対象操作が取り消される |
| HIST-08 | 5.11 | A | Undo 後に新しい履歴対象操作を行い Ctrl+Y を押す | やり直し履歴が消えており、古い操作は Redo されない |
| HIST-09 | 5.11 | B | C1 で操作し C2 へ切り替えた後 Ctrl+Z／Ctrl+Y する | 履歴がキャンバス切り替え後も保持され、対象キャンバスの操作が正しく戻る |
| HIST-10 | 5.11 | C | 51回以上の履歴対象操作を行い、連続で Undo する | 保持される履歴は新しいものから最大50件で、アプリが停止しない |
| HIST-11 | 5.9, 5.11 | A | 履歴がある状態で手動復元に成功し Ctrl+Z／Ctrl+Y を試す | Undo、Redo 両方の履歴が空になっており、復元前の操作は再現されない |

## 11. 保存・復元・未保存状態

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| SAVE-01 | 5.9 | A | 各種データを作り、保存ボタンと Ctrl+S をそれぞれ実行する | 保存成功が表示され、未保存状態が解除される。ブラウザの保存ダイアログは開かない |
| SAVE-02 | 5.9 | A | 保存後に Application の Local Storage を確認する | キーが正確に `d4soft01.todoCanvas.state` で、値はJSON文字列である |
| SAVE-03 | 5.9 | A | 保存JSONのトップレベルと配下を確認する | `version`、`currentCanvasId`、`canvases`、`viewSettings` があり、仕様記載のキャンバス・タスク・接続・表示設定の全属性を持つ |
| SAVE-04 | 5.9 | A | C1/C2、タスク、接続、位置、検索、ステータス、深さ設定を保存して再読み込みする | 全データ、現在キャンバス、表示設定が復元され、未保存なしで起動する |
| SAVE-05 | 5.9 | A | タスク登録、編集、移動、削除、接続作成／削除、フィルター変更、パン、キャンバス削除をそれぞれ行い、保存前に Local Storage を確認する | 画面は未保存表示になるが、既存の保存データは自動更新されない |
| SAVE-06 | 5.9 | A | C1 を変更後、C2 に切り替える | 切り替え前保存が行われ、成功時は C2 に切り替わる。保存JSONの `currentCanvasId` も C2 である |
| SAVE-07 | 5.9 | A | 開発者ツール等で localStorage 書き込み失敗を再現し、保存とキャンバス切り替えを試す | 保存失敗を表示し、既存保存値を維持する。切り替え時なら C1 のままで、未保存状態も維持する |
| SAVE-08 | 5.9 | A | 保存済み状態から未保存の変更を加え、手動復元を実行して確認でキャンセルする | 復元せず未保存の画面状態を維持する |
| SAVE-09 | 5.9 | A | SAVE-08 と同じ状態で、破棄確認を承認する | 保存時点へ戻り、未保存なしとなり、選択や履歴など保存対象外の矛盾がない |
| SAVE-10 | 5.9 | A | 保存データがない状態で手動復元する | アプリが停止せず初期状態となるか、復元対象なしを明確に表示する。既存画面を無言で破壊しない |
| SAVE-11 | 5.9, 5.13 | A | 保存値へ不正JSONを設定して再読み込みする | 初期状態で起動し復元失敗を表示する。不正な保存値自体は削除・上書きしない |
| SAVE-12 | 5.9, 5.13 | A | `version` を未知値、または必須項目を1つ欠落させて再読み込みする | 形式不正として初期状態で起動し、元の保存値は残る |
| SAVE-13 | 5.9 | A | `currentCanvasId` を存在しないID、またはキャンバス0件で非null相当の値に改変する | 復元失敗となり、アプリは操作可能な初期状態で起動する |
| SAVE-14 | 5.9 | A | キャンバス／タスク／接続のID重複、空タイトル、不正ステータス、NaN相当・無限相当の座標を個別に含む保存値を読み込む | 各データを不正として拒否し、一部だけを補正して復元しない |
| SAVE-15 | 5.9 | A | 自己接続、存在しないタスク参照、別キャンバスのタスク参照、同方向重複接続を個別に含む保存値を読み込む | すべて復元失敗となり、画面停止しない |
| SAVE-16 | 5.9 | A | 深さ有効で基準ID不正、最大深さが負数／小数、ステータス絞り込みが未知値の保存値を読み込む | すべて復元失敗となる |
| SAVE-17 | 2.4, 5.9 | B | 正常保存JSONの日時とIDを確認する | IDは種別・現在日時・乱数を組み合わせた一意な文字列、日時はISO 8601文字列で、作成日時は編集・移動・保存後も不変である |
| SAVE-18 | 5.9 | B | localStorage を容量上限まで使用するなどして書き込みを失敗させる | 保存失敗を表示し、アプリ上のデータは操作可能なまま、保存済みデータを不完全なJSONで上書きしない |

## 12. キーボード・マウス操作

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| KEY-01 | 4.2 | A | Ctrl+A、Ctrl+Z、Ctrl+Y、Ctrl+S、Ctrl+X、Ctrl+F、Ctrl+Shift+F、Ctrl+G を通常モードで試す | 各操作が仕様どおり動作し、ブラウザ標準操作との競合時はアプリ操作が優先される |
| KEY-02 | 4.2 | A | 選択中タスクで Ctrl+C、Ctrl+V、Ctrl+E、Ctrl+D、Delete、↑、↓ を試す | コピー、貼り付け、編集、削除、前後のタスク選択が仕様どおり動作する |
| KEY-03 | 4.2 | B | タスク未選択、接続線未選択で Delete、Ctrl+E、Ctrl+D、↑、↓ を押す | データを変更せず、例外も発生しない |
| KEY-04 | 4.2 | A | 入力欄へフォーカスし Ctrl+A/C/V、Enter、Esc を試す | 文字列選択・コピー・貼り付けが優先され、説明文 Enter は改行、Esc は編集キャンセルとなる |
| KEY-05 | 4.2 | B | キーボード操作時に Console とブラウザ挙動を確認する | アプリ用操作とブラウザ標準操作が同時実行されず、未処理例外がない |
| MOUSE-01 | 4.3 | A | 空白のクリック、ダブルクリック、右クリック、ドラッグを試す | 選択解除、タスク登録、キャンバスメニュー、パンにそれぞれ正しく振り分けられる |
| MOUSE-02 | 4.3 | A | タスクのクリック、ダブルクリック、右クリック、ドラッグ、ホバーを試す | 選択、編集、タスクメニュー、移動、ホバー表示にそれぞれ正しく振り分けられる |
| MOUSE-03 | 4.3 | B | 接続線をクリックし、線から少し外れた空白もクリックする | 線上だけで接続選択になり、空白操作との誤判定が実用上ない |

## 13. エラー表示・入力安全性

| ID | 仕様節 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| ERR-01 | 5.12 | A | 登録、編集、接続、深さ、保存、復元の各失敗を発生させる | メニューバーまたは対象付近に、原因を判断できる短いエラーが表示される |
| ERR-02 | 5.12 | B | エラー表示後に正しい操作を成功させる | 以前のエラーが消え、必要に応じて成功表示に変わる |
| ERR-03 | 5.12 | B | 保存成功後にタスクまたは表示設定を変更する | 保存成功表示が未保存表示へ戻る |
| SEC-01 | 5.13 | A | HTML風・script風文字列をタスクタイトル、説明、キャンバスタイトルに入力して表示する | タグが文字として表示され、画像読込、alert、DOM追加、スクリプト実行が起きない |
| SEC-02 | 5.13 | A | HTML風文字列を検索欄へ入力する | 文字列として検索され、HTMLとして解釈されない |
| SEC-03 | 5.13 | A | 保存JSON内のタイトル・説明を script風文字列にして正常な構造として復元する | 文字列として表示され、スクリプトは実行されない |
| SEC-04 | 5.13 | B | Elements でユーザー入力部分を確認する | 入力内容が意図しないHTML要素として生成されていない |

## 14. 非機能・総合シナリオ

| ID | 根拠 | 優先度 | 操作・確認手順 | 期待結果 |
|---|---:|:---:|---|---|
| NF-01 | req 4.1 | B | 初見の利用者に、説明なしでキャンバス作成→タスク追加→移動→接続→完了→フィルターを依頼する | 操作入口と現在モードが認識でき、重大な迷い・誤操作なく完了できる |
| NF-02 | req 4.2 | B | 50件程度のタスクと接続を作り、ドラッグ、検索、深さ変更、パンを行う | 体感上大きな遅延、ちらつき、操作取りこぼし、画面停止がない |
| NF-03 | req 4.3 | A | 循環接続、不正保存データ、存在しない参照を含むデータを扱う | アプリが停止せず、安全な初期状態または明示的エラーになる |
| NF-04 | req 4.5 | C | 最新 Chrome、Edge、Firefox で主要A項目を実施する | ブラウザ間で主要動作とレイアウトに実用上の差がない |
| NF-05 | req 4.5 | C | 幅320px程度、標準PC幅、大画面で表示し、閲覧と編集を行う | 狭い画面でも最低限の閲覧・編集ができ、重要操作が画面外へ失われない |
| NF-06 | req 4.6 | B | Tab／Shift+Tab でボタン・入力欄を移動し、ラベル、フォーカス表示、状態表示を確認する | 操作対象と入力欄の意味が分かり、基本的な追加・編集がキーボードで完了できる |
| NF-07 | req 4.6 | B | グレースケール表示または色覚シミュレーションで状態を確認する | ステータス、選択、完了を色だけでなく文字や形でも識別できる |
| E2E-01 | spec 全体 | A | 初期化→C1/C2作成→A～F登録→接続グラフ作成→編集・移動→3フィルター併用→保存→再読み込み→C2切り替え→C1復帰→削除→Undo/Redo の順で実施する | 各段階で状態、件数、線、表示設定、未保存表示が整合し、保存後の再現性とキャンバス間分離が維持される |

## 15. 実施順序の提案

すべて実施する場合は、依存関係を考慮して次の順序が効率的である。

1. `INIT`、`UI`、`CAN`
2. `TASK`、`EDIT`、`MOVE`、`DEL`
3. `MODE`、`CON`
4. `FIL`
5. `HIST`
6. `SAVE`
7. `KEY`、`MOUSE`、`ERR`、`SEC`
8. `NF`、`E2E`

時間が限られる場合は、まず優先度Aと `E2E-01` を実施し、その後に優先度B、Cを追加する。

## 16. 仕様差異を見つけた場合の記録例

```text
項目ID: CAN-01
結果: NG
期待結果: 初期タイトルが「新規キャンバス」になる
実際の結果: 「新しいキャンバス」と表示された
再現率: 3/3
環境: Chrome xxx / Windows 11
証跡: screenshot_CAN-01.png
備考: spec.md 2.4、5.10を参照
```

表記差、ボタン不足、ショートカット不足も「近い動作をするためOK」とせず、仕様との差として記録する。
